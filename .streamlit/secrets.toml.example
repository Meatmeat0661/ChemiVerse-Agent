# 在 share.streamlit.io → App → Settings → Secrets 中粘贴（可选，覆盖默认路径）
# 若不填，使用仓库内 config.cloud.yaml + data/*.json

[data]
molecules_path = "data/chemiverse_species.json"
reactions_path = "data/chemiverse_reactions.json"
